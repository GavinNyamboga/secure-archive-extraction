schema_version = "1.3"
artifacts = []

[task]
name = "dev-gavin/secure-archive-extraction"
description = "Repair an unsafe tar extraction utility so that it prevents path traversal, unsafe archive members, conflicting paths, and partial extraction."
authors = [
    { name = "Gavin Nyamboga", email = "gavinnyamboga3@gmail.com" },
]
keywords = [
    "python",
    "tar",
    "archive-extraction",
    "path-traversal",
    "security",
]

[metadata]

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]

[agent]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
